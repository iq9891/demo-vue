## 创建大型应用demo ##

1. 安装node
2. 输入如下代码
```
npm install
```
3. 开发环境，预览地址 http://localhost:8080/webpack-dev-server/ 或者 http://localhost:8080 ，输入如下代码
```
npm run dev
```
4. 发布代码，直接点击createBigApp/index.html预览，输入如下代码
```
npm run build
```